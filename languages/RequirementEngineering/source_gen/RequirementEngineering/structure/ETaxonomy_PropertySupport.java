package RequirementEngineering.structure;

/*Generated by MPS */

import jetbrains.mps.smodel.PropertySupport;
import java.util.Iterator;
import jetbrains.mps.internal.collections.runtime.ListSequence;

public class ETaxonomy_PropertySupport extends PropertySupport {
  public boolean canSetValue(String value) {
    if (value == null) {
      return true;
    }
    Iterator<ETaxonomy> constants = ListSequence.fromList(ETaxonomy.getConstants()).iterator();
    while (constants.hasNext()) {
      ETaxonomy constant = constants.next();
      if (value.equals(constant.getName())) {
        return true;
      }
    }
    return false;
  }
  public String toInternalValue(String value) {
    if (value == null) {
      return null;
    }
    Iterator<ETaxonomy> constants = ListSequence.fromList(ETaxonomy.getConstants()).iterator();
    while (constants.hasNext()) {
      ETaxonomy constant = constants.next();
      if (value.equals(constant.getName())) {
        return constant.getValueAsString();
      }
    }
    return null;
  }
  public String fromInternalValue(String value) {
    ETaxonomy constant = ETaxonomy.parseValue(value);
    if (constant != null) {
      return constant.getName();
    }
    return "";
  }
}
